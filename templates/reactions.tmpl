{{with .Data}}
{{template "header.tmpl" (WithContext .CommonData $.Ctx)}}
<div class="page-title"> Reactions </div>

{{ range $k, $v := .Map }}
{{ $number := len $v }}
<div class="reaction-list-title">{{$k}} ({{$number}})</div>
{{template "userlist.tmpl" (WithContext $v $.Ctx)}}
{{end}}

{{template "footer.tmpl"}}
{{end}}